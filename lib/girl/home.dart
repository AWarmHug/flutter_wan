import 'package:flutter/material.dart';

class GirlHome extends StatelessWidget {
  const GirlHome({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Girl"),
      ),
    );
  }
}
